from brownie import Lottery, accounts, network, config
from web3 import Web3


def test_get_entrace_fee():
    # account = accounts[0]
    # print(f"ntwork {network.show_active()}")
    # print(
    #     f'sssssssssss {config["networks"][network.show_active()]["eth_usd_price_feed"]}'
    # )
    # lottery = Lottery.deploy(
    #     config["networks"][network.show_active()]["eth_usd_price_feed"],
    #     {"from": account},
    # )
    # entrance_fee = lottery.getEntranceFee()
    # print(f"entrance_fee {entrance_fee}")

    # assert entrance_fee > Web3.toWei(0.016, "ether")
    # assert entrance_fee < Web3.toWei(0.017, "ether")
    pass
